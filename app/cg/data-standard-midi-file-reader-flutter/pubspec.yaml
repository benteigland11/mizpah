name: standard_midi_file_reader
description: Standard Midi File Reader - a Cartograph widget.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # --- cartograph-deps start (generated from widget.json - do not edit) ---
  # --- cartograph-deps end ---
  # --- cartograph-composed start (blueprint deps - do not edit) ---
  # --- cartograph-composed end ---

dev_dependencies:
  flutter_test:
    sdk: flutter
