Forked into this workspace at 7c70a9a (see its message for the upstream commit).
diverged: the contamination scan's unlisted-import check no longer depends on what the validating interpreter has installed — well-known package→import aliases (Pillow→PIL, opencv-python→cv2, PyYAML→yaml, …) are known without installing, hyphens normalise to underscores, and an import a declared-but-uninstalled package might provide is a warning, not a block (a widget declaring Pillow passed in the sandbox and was blocked on the host, 2026-09-23)
diverged: `inspect` carries the widget's API — public functions and classes with their signatures and first docstring line (Python by ast, other languages by declaration lines) — and full source stays opt-in (--source); workers asked for source in 76 of 93 inspects to learn how to call a widget, at ~6K characters each against ~1.5K (2026-09-23)
