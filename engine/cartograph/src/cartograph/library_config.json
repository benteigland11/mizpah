{
  "domains": {
    "backend": "Request handling, APIs, databases, queues, caching.",
    "data": "Transformation, parsing, serialization, pipelines, file I/O.",
    "ml": "Model loading, inference, embeddings, training utilities.",
    "security": "Authentication, authorization, encryption, input sanitization.",
    "infra": "Deployment, config management, logging, monitoring, health checks.",
    "frontend": "UI rendering, templating, client-side logic.",
    "universal": "Pure utilities with no domain dependency (string ops, retry, math, date handling). If a widget could reasonably belong to two domains, it belongs here.",
    "modeling": "Parametric and generative representations of physical or visual things — 3D geometry, surfaces, shapes, simulations. Widgets produce artifacts (STL, mesh, image) rather than running as services.",
    "rtl": "Register-transfer level hardware design — synthesizable digital logic modules. Widgets are parameterized RTL modules in SystemVerilog (.sv) that compile and simulate cleanly. No vendor primitives, no simulation-only constructs in src/.",
    "devops": "Infrastructure-as-code, deployment, and operational tooling. Widgets are reusable cloud/on-prem patterns (Terraform modules, IAM policies, network primitives, deployment topologies) whose value compounds as more consumers harden them. Validation is shape-only: declarations parse and references resolve. Runtime behavior is the consumer's responsibility.",
    "analog": "Analog and mixed-signal circuit design — parametric SPICE netlists validated by simulation. Widgets are reusable circuit blocks (filters, amplifiers, references, oscillators) proven by a testbench that simulates to convergence and asserts measured quantities (cutoff frequency, gain, settling time) within tolerance. Like modeling, the value is an artifact-style proof rather than a running service; like rtl, every component value is a parameter, not a hardcoded constant.",
    "gamedev": "Game development logic and systems — reusable, engine-validated gameplay code (state machines, inventory, pathfinding helpers, input mapping, save/load, procedural generation). Widgets are self-contained scripts proven by a headless test that loads the script, exercises its API with fake data, and asserts behavior — no editor, no scene assumptions. The value is portable game logic that drops into any project, not a specific scene tree.",
    "formal": "Formally verified code and machine-checked proofs - definitions that ship with kernel-checked theorems about their behavior (verified algorithms, data structures, protocol properties)."
  },
  "general_notes": "Single responsibility - one widget does one thing. No global state. No project-specific code, hardcoded paths, credentials, or environment-specific values. Expose variation points as constructor parameters or function arguments - no hardcoded thresholds, colors, sizes, or behavior flags. URLs and endpoints belong in examples as demonstration data, not in src/ - if a widget needs a URL to operate, it must be a parameter the caller provides. All dependencies must be declared in widget.json tech_stack.dependencies. Widgets cannot depend on other widgets - they must be fully self-contained. If you need logic from another widget, copy it into src/ directly. Examples must run and exit cleanly with no user input, no network calls, and no external services or API keys - use fake/hardcoded data. The widget's own declared dependencies are fine and will be installed by the validator before the example runs. Do not modify library_notes - it is managed by Cartograph and will be restored on checkin.",
  "language_notes": {
    "python": "No project-specific names, identifiers, or terminology anywhere in src/, tests/, or examples/ - use generic names like 'user', 'item', 'value', 'example_org'. A test that references your project's real entity names is contaminated. Use pytest for all tests - no unittest. Type hints required on all public functions. No __init__.py in tests/. No if __name__ == '__main__' blocks in src/ files. Imports ordered: stdlib, third-party, local. ML domain: framework-free (numpy, scikit-learn, plain Python) and API-based inference (HTTP calls to OpenAI, Anthropic, HuggingFace Inference API) work everywhere. Heavy optional frameworks must be pre-installed - Cartograph will not auto-install them. Declare dependencies in widget.json normally; consumers must also have them installed to validate.",
    "javascript": "No project-specific names, identifiers, or terminology anywhere in src/, tests/, or examples/ - use generic names like 'user', 'item', 'value', 'example.com'. A test that references your project's real entity names or domain is contaminated. Use vitest for all tests. Include a vitest config and a coverage provider (@vitest/coverage-v8 or @vitest/coverage-istanbul) in devDependencies - the validator enforces 80% coverage via CLI flags but does not inject test configuration. No console.log() in src/ files. Use named exports, not default exports. Test filename must match src filename exactly. Only test/expect/describe/vi are global - all other imports must be explicit. Plain JS: files use .js, examples run with node and exit cleanly. Frontend (DOM) testing: add 'happy-dom' to devDependencies and set environment: 'happy-dom' in your vitest config - the scaffold does this automatically for frontend widgets. Canvas/WebGL testing: add '@vitest/browser' and 'playwright' to devDependencies and configure the browser provider in your vitest config - Playwright must be installed on the system (run 'npx playwright install chromium'). React is not scaffolded by default. To use React: add react and react-dom to widget.json tech_stack.dependencies and package.json dependencies; rename files to .jsx; add @vitejs/plugin-react, @testing-library/react, and jsdom to package.json devDependencies; configure your vitest config with the react plugin and jsdom environment. Examples with a non-.js extension (.jsx, .ts, .tsx) run via `npx tsx` - no bundler step. Test environment is entirely your vitest config's responsibility - Cartograph does not inject React dev-deps. Components must be PascalCase .jsx, hooks camelCase .js starting with 'use'. Components must render cleanly via react-dom/server renderToString with no browser APIs (window, document, localStorage) in the initial render path. Usage hints: you may optionally include examples/usage_hint.* with real integration code that requires a browser or app context. These are not executed by Cartograph.",
    "typescript": "Follows the same conventions as JavaScript widgets (vitest for all tests, a vitest config with a coverage provider in devDependencies, 80% coverage, named exports not default, no console.log in src/, only test/expect/describe/vi global, test filename matches src filename), with TypeScript on top. No project-specific names anywhere - use generic names like 'user', 'item', 'value', 'example.com'. Source files are .ts; annotate the public surface (exported function params and returns) - the types ARE the widget's contract. Prefer precise types over `any`. The package.json must compile under the scaffolded tsconfig. Examples are .ts and run via `npx tsx` with no bundler step - they must run and exit cleanly with no user input, no network calls, and no external services (use fake/hardcoded data). Declared dependencies in widget.json tech_stack.dependencies are installed before validation.",
    "openscad": "No project-specific names, part numbers, or assembly identifiers in src/, tests/, or examples/ - use generic descriptive names. Use module for all reusable geometry — never write top-level statements outside a module in src/, including calls to your own modules (no preview instantiations). All parameters must have defaults so the module renders on its own. Every src/*.scad file must declare a coordinate frame at the top using one or more // COORDINATES (cartesian|cylindrical|spherical): blocks. At least one block must be fully filled in (no placeholders) with all required fields. Cartesian requires Origin, +X, +Y, +Z. Cylindrical requires Axis, Origin, theta. Spherical requires Origin, theta, phi. Right-hand rule is assumed across all three systems. Multiple frames are allowed when a part is genuinely dual (e.g. a gear that's both cylindrical-by-rotation and cartesian-by-mounting); delete frames you don't use. All dimensions are millimeters and angles are degrees unless explicitly noted in a parameter's unit comment. Tests are .scad files that call your module with different parameters — each must render to a non-empty STL without errors. No coverage requirement: passing renders is the bar. Examples must render cleanly with no user input and no external services. Dependencies (e.g. BOSL2) must be declared in widget.json but are not auto-installed — consumers must add them to their OpenSCAD library path manually. Importing libraries: use use<> for libraries that only provide modules/functions (no side effects). Use include<> for libraries like BOSL2 that expose constants and special variables — include<> is allowed in src/ for declared dependencies. Standard BOSL2 pattern: include <BOSL2/std.scad> then include <BOSL2/gears.scad> (or whichever submodule). Tests and examples that call your module should use include <../src/your_module.scad> (not use<>) when your src file uses include<> for a library — use<> does not forward include<> statements transitively, so the library constants will be missing at render time. Optional Python sidecar: an openscad widget may carry a python/ directory with stdlib-only calc helpers (printability checks, gear ratios, threading math). Filenames mirror src/ (src/threads.scad ↔ python/threads.py + python/test_threads.py). Sidecars validate at 60% coverage and may not declare dependencies — stdlib only. Delete python/ if not used.",
    "systemverilog": "No project-specific names, signal names tied to a particular design, or system identifiers in src/, tests/, or examples/ - use generic RTL names. Use SystemVerilog (.sv) for all files. Testbenches use $fatal(1, \"message\") to signal failures and $finish to exit cleanly — vvp exits non-zero on $fatal, so it maps directly to test pass/fail. `timescale belongs in testbenches, never in src/. No `initial` blocks, #delays, or $display/$monitor in src/ — these are simulation-only constructs. Vendor primitives (LUT6, BUFG, ALTPLL, etc.) are blocked unless the vendor library is declared in dependencies — generic RTL is the default. Use always_ff for sequential logic, always_comb for combinational — `always @(...)` is blocked; we enforce SystemVerilog idioms. All tunable values (clock frequency, baud rate, data width, address width) must be module parameters with defaults and unit comments. Parameters are the variation points: a consumer should be able to instantiate your module with different parameters without touching source. Reset must be a parameter-defined polarity (active-high or active-low) with synchronous behavior as the default. No hardcoded sizes or widths — everything that varies by use case must be a parameter. $readmemh/$readmemb file paths must be parameters, not hardcoded strings.",
    "angular": "No project-specific names, identifiers, or terminology anywhere in src/, tests/, or examples/ - use generic names like 'item', 'value', 'example.com'. A test or component that references your project's real entity names or domain is contaminated. Use Angular standalone components (@Component with standalone: true) - avoid NgModule boilerplate. Use ng test --watch=false for all tests - Karma + Jasmine runs in ChromeHeadlessNoSandbox (configured in karma.conf.js). Coverage threshold is 80% and is enforced via karma.conf.js check.global - do not lower these values. Declare additional npm dependencies in widget.json tech_stack.dependencies as '@scope/package >= version' or 'package >= version'. No console.log() in src/ files. Use named exports, not default exports. No process.exit() in src/. Example validation deviates from standard: examples cannot be executed as scripts - instead, ng build compiles the library and type-checks examples/ via tsconfig.lib.json. The example must compile without TypeScript errors; it does not need to run. URLs and endpoints belong in examples as demonstration data, not in src/ - accept them as @Input() parameters. Angular-specific contamination: component selectors must be generic (lib-item, lib-value) - not project-branded (lib-mycompany-header). Decorator metadata (@Component selector, templateUrl) must not contain project-specific strings.",
    "nim": "No project-specific names, identifiers, or terminology anywhere in src/, tests/, or examples/ - use generic names like 'item', 'value', 'exampleUser'. Use std/unittest for all tests (import std/unittest, not bare import unittest). Use func for pure procedures with no side effects — only use proc when side effects are required. Mark all public symbols with * (e.g. func myFunc*(x: int): int). Use camelCase for funcs/procs/variables, PascalCase for types. Prefer stdlib imports with the std/ prefix (e.g. import std/strutils, not import strutils). No echo in src/ files. No quit() or system.quit() in src/ files. No {.importc.} or {.compile.} pragmas — C FFI makes widgets platform-dependent and unvalidatable. No {.global.} pragma — widgets must not use global mutable state. No OS-targeting when defined(windows/linux/posix/etc.) — widgets must validate on all platforms. No cast[seq[T]] in src/ — GC-managed seq casts break GC invariants and are blocked; copy explicitly or use toOpenArrayByte. Avoid bare `except:` — catches Defect and KeyboardInterrupt; use a typed except like `except CatchableError:`. Avoid raw memory primitives (alloc/dealloc/copyMem, cast[ptr ...], ptr UncheckedArray) — prefer GC-managed seq/string; warns if used. A widget_id.nimble file is required at the widget root - it is created by cartograph create and must stay in sync with widget.json dependencies. List nimble package dependencies in widget.json tech_stack.dependencies as 'pkgname >= version'.",
    "php": "No project-specific names, identifiers, or terminology anywhere in src/, tests/, or examples/ - use generic names like 'item', 'value', 'example.com'. A test or class that references your project's real entity names or domain is contaminated. Use PHPUnit for all tests - require phpunit/phpunit in composer.json require-dev. Use PHP 8.1+ features (named arguments, enums, fibers, readonly properties). No echo in src/ files - use return values only. No WordPress globals anywhere (wp_*, add_action, add_filter, $wpdb, $post, $wp_query, get_option, register_post_type, etc.) - PHP widgets must be pure PHP with no framework dependency. Coverage is enforced at 80% via PHPUnit --min-coverage - Xdebug or PCOV must be installed. Declare Composer dependencies in widget.json tech_stack.dependencies as 'vendor/package >= version'. A composer.json and phpunit.xml are required at the widget root - created by cartograph create. Namespace all classes under Cartograph\\\\YourWidget\\\\. No die() or exit() in src/ files. URLs and endpoints belong in examples as demonstration data, not in src/ - accept them as constructor parameters or method arguments.",
    "terraform": "No project-specific names, account IDs, or environment identifiers anywhere in src/, tests/, or examples/ - use generic names like 'name', 'environment', 'tags'. Layout: src/ holds the module (main resource files, variables.tf, outputs.tf, versions.tf), tests/ holds at least one root configuration that calls the module with sample inputs to prove external callability, examples/ holds a root configuration showing typical use. Validation is shape-only: terraform validate runs against src/, tests/, and examples/. There is no test runner and no coverage requirement - widgets harden through use. Modules must NOT declare provider blocks or backend blocks in src/ - those belong to the consumer's root configuration. Modules MUST declare required_version and required_providers in src/versions.tf so consumers know the contract. All variation points must be input variables with descriptions and types - never hardcode region, account ID, instance size, or naming prefix. Real AWS account IDs in arn:aws:* strings are blocked - use placeholders (000000000000) or accept as a variable. Hardcoded credentials (access_key, secret_key, password, api_key) are blocked anywhere. terraform init runs with TF_PLUGIN_CACHE_DIR set so providers download once across src/, tests/, and examples/. Provider versions are pinned in versions.tf - widget.json tech_stack.dependencies is not used to pin Terraform providers.",
    "spice": "No project-specific names, part numbers, or schematic identifiers anywhere in src/, tests/, or examples/ - use generic circuit-function names (lowpass_rc, inverting_amp, bandgap_ref). Source files are SPICE netlists (.cir). src/ must define a reusable .subckt block and nothing else: no analysis cards (.ac/.dc/.tran/.op/...) and no .control blocks - those belong in the testbench. Every component value (R/L/C, source, bias) must be a {param} so a consumer can retune the block without editing source - hardcoded literals in a .subckt warn. The testbench (tests/test_<module>.cir) includes the block via `.include ../src/<module>.cir`, drives it with a stimulus, runs an analysis inside a .control block, and proves behavior: it must contain at least one `meas` and assert each measured quantity with an `if (...) echo \"ASSERT_FAIL ...\" else echo \"ASSERT_PASS ...\" end` block. There is no line-coverage requirement - the coverage floor is that every claimed output is measured and asserted within a tolerance band; prefer banded asserts (cutoff within +/-5%) over exact-value checks because convergence is sensitive across ngspice versions. Validation runs `ngspice -b` and parses output, not the exit code (ngspice returns 0 even when a .meas fails): a run fails on any Error/aborted/fatal/failed! marker, on any ASSERT_FAIL, or when no ASSERT_PASS is produced. Examples must simulate to convergence and exit cleanly. Device-model libraries (.lib/.include of external models) must be declared in widget.json dependencies and supplied by the consumer - Cartograph does not fetch them; absolute-path and machine-specific includes are blocked.",
    "java": "No project-specific names, URLs, or environment identifiers anywhere in src/, tests/, or examples/ - use generic names. Layout: settings.gradle + build.gradle at the widget root, src/ holds the library sources (package <widget_slug_with_underscores>), tests/ holds black-box JUnit 5 tests (package tests, files end in Test.java, import the widget by package), examples/ holds ExampleUsage.java (package examples, a main() run via the scaffolded `runExample` Gradle task - it must run and exit cleanly with no user input, no network calls, no external services). The widget OWNS its build.gradle including the plugins block (build-time plugins like Fabric Loom are allowed), but the build must keep two contracts: `test` produces a JaCoCo XML report (xml.required = true) and `runExample` runs the example. Tests run with `gradle test` and JaCoCo line coverage of src/ must reach 80%. Dependencies are declared in widget.json as 'group:artifact>=version' (e.g. 'com.google.code.gson:gson>=2.11.0') and written to cartograph-deps.gradle by the validator - never edit that file or hand-add coordinates to build.gradle's dependencies block. Widgets are libraries: no System.out/System.err printing, no System.exit/Runtime.halt, no Thread.sleep in src/ (all blocked); System.getenv/getProperty, non-final static fields, hardcoded numeric constants, unlisted package imports, and hardcoded URLs/IPs/paths/credentials produce warnings. src/ must compile via `gradle compileJava`.",
    "csharp": "No project-specific names, URLs, or environment identifiers anywhere in src/, tests/, or examples/ - use generic names. Layout: cartograph-deps.props at the widget root (generated from widget.json deps - never edit), src/ holds the class library (src/<widget_id>.csproj, sources in namespace <PascalCaseSlug>Widget), tests/ holds black-box xUnit tests (tests/Tests.csproj, files end in Tests.cs, import the widget by namespace), examples/ holds ExampleUsage.cs (examples/Examples.csproj executable, top-level statements - it must run and exit cleanly with no user input, no network calls, no external services). The widget OWNS its csproj files (analyzers and source generators are allowed), but the build must keep two contracts: `dotnet test tests` produces a Cobertura coverage report via coverlet.collector, and `dotnet run --project examples` runs the example. Tests run with `dotnet test` and coverlet line coverage of src/ must reach 80%. Dependencies are declared in widget.json as 'PackageId>=version' (e.g. 'Newtonsoft.Json>=13.0.3') and written to cartograph-deps.props by the validator - never edit that file or hand-add PackageReferences to the src csproj. Widgets are libraries: no Console printing, no Environment.Exit/FailFast, no Thread.Sleep in src/ (all blocked); Environment.GetEnvironmentVariable, mutable static fields, hardcoded numeric constants, unlisted using imports, and hardcoded URLs/IPs/paths/credentials produce warnings. src/ must build via `dotnet build src`. Requires .NET SDK 10+.",
    "flutter": "No project-specific names, identifiers, or terminology anywhere in src/, tests/, or examples/ - use generic names like 'item', 'value', 'example.com'. Layout: pubspec.yaml at the widget root (the widget OWNS it, but the two marked blocks inside dependencies: are generated - never edit between the cartograph-deps / cartograph-composed markers, and never remove them), src/ holds the library sources (import sibling src/ files by RELATIVE path only), tests/ holds flutter_test tests (files end in _test.dart and import the widget as package:<module>/<module>.dart), examples/ holds example_usage.dart. lib/ is a GENERATED copy of src/ made at validation time because Flutter's coverage collector only reports libraries under lib/ - never write to lib/ or commit it, and never add files there. Example validation deviates from standard: Flutter UI code cannot run on the bare Dart VM, so examples execute through the flutter_test harness (flutter test examples/example_usage.dart) and must wrap their demonstration in at least one test()/testWidgets() block with fake/hardcoded data - no user input, no network calls, no external services. Tests run with flutter test and lcov line coverage of src/ must reach 80%. Widgets may be Flutter UI or pure Dart logic; prefer importing package:flutter/widgets.dart over material.dart so consumers choose their design system. Dependencies are declared in widget.json as 'package>=version' (e.g. 'collection>=1.18.0') and written into pubspec.yaml by the validator. Widgets are libraries: no print/debugPrint/stdout output, no exit(), no blocking sleep() in src/ (all blocked); Platform.environment, top-level numeric consts, unlisted package imports, and hardcoded URLs/IPs/paths/credentials produce warnings. src/ must pass flutter analyze with no errors.",
    "go": "No project-specific names, URLs, or environment identifiers anywhere in src/, tests/, or examples/ - use generic names. Layout: go.mod at the widget root (module <widget-slug>), src/ holds the library package (import path '<module>/src'), tests/ holds black-box tests (package tests, files end in _test.go, import the widget by module path), examples/ holds example_usage.go (package main, run with `go run ./examples`). Tests run with `go test ./tests/...` and coverage of src/ must reach 80% (-coverpkg=./src/...). Dependencies are declared in widget.json as '<module-path>>=<version>' (e.g. 'github.com/google/uuid>=1.6.0') and pinned into go.mod by the validator. Widgets are libraries: no fmt.Print*/println, no os.Exit/log.Fatal, no panic in init() in src/ (all blocked); time.Sleep, os.Getenv, top-level mutable vars, and hardcoded URLs/IPs/paths/credentials produce warnings. go vet and a build of src/ must pass. Modern-standards enforcement: code must be gofmt-clean (hard block) and free of the deprecated io/ioutil import (use io/os); math/rand (prefer math/rand/v2), interface{} (use any), undocumented exported identifiers, anonymous goroutines without lifecycle management, and error comparisons with ==/!= (use errors.Is) all warn.",
    "rust": "No project-specific names, URLs, or environment identifiers anywhere in src/, tests/, or examples/ - use generic names. Layout: Cargo.toml at the widget root (package name <widget-slug>), src/ holds the library crate (src/lib.rs), tests/ holds integration tests (files end in .rs, import the widget by its crate name as a consumer would), examples/ holds example_usage.rs (run with `cargo run --example example_usage`). Tests run under cargo-llvm-cov and line coverage of src/ must reach 80% (tests/ and examples/ are excluded from the denominator). Dependencies are declared in widget.json as '<crate>>=<version>' (e.g. 'serde>=1.0.0') and added to Cargo.toml by the validator; Cargo.lock pins the resolved versions. Widgets are libraries: no println!/print!/eprintln!/eprint!, no process::exit/abort, no unsafe in src/ (all blocked); thread::sleep, env::var, todo!/unimplemented!, undocumented public items, and hardcoded URLs/IPs/paths/credentials produce warnings. A `cargo build --lib` must succeed. Modern-standards enforcement: source must be rustfmt-clean (`cargo fmt --check`, hard block). Requires the cargo-llvm-cov subcommand (cargo install cargo-llvm-cov + the llvm-tools-preview component) for the coverage gate.",
    "gdscript": "Godot 4 only - deprecated Godot 3 syntax is hard-blocked (use @onready/@export/@tool not the bare keywords, await not yield, Callable-based signal.connect not the 3-arg form, Packed*Array not Pool*Array, the renamed nodes CharacterBody*/Node3D/DirAccess/RefCounted, .instantiate() not .instance(), randf_range/is_empty). No project-specific names, URLs, or environment identifiers anywhere - use generic names. Layout: project.godot at the widget root (a minimal project so res:// resolves), src/ holds the reusable script(s) with a PascalCase `class_name`, tests/ holds test_<name>.gd, examples/ holds example_usage.gd. Tests and examples are `extends SceneTree` scripts run headless with `godot --headless --path . --script <file>`; a test must load the widget via `res://src/...`, assert behavior with fake data, print `ASSERT_PASS` for each verified property, and quit(non-zero) on failure - a clean exit with no ASSERT_PASS is rejected (Godot can exit 0 even when logic is wrong). Gotcha: `SceneTree.quit()` does NOT return from the function - it only requests quit at end of frame, so execution keeps running and a later quit() overrides an earlier one; always `return` right after quit() (or make it the last statement). There is NO line-coverage requirement (no coverage tool exists for GDScript) - add one as a custom rule if your team wants it. Widgets must be scene-tree-agnostic: no print*/print_debug, no absolute node paths (get_node(\"/root/...\"), $/...), and no OS.delay_* in src/ (all blocked); untyped vars, hardcoded numeric const tunables, OS.get_environment, TODO/FIXME, and hardcoded URLs/IPs/paths/credentials produce warnings. Static typing is the house style - annotate parameters, returns, and members. GDScript widgets have no external package dependencies (pure engine builtins + their own scripts).",
    "lean": "Lean 4 only. The only supported dependency is `mathlib` (declare it in widget.json dependencies); it builds against a shared pinned workspace that must be provisioned once with `cartograph setup-mathlib` - it is never downloaded automatically. All other widgets build against the Lean core library and declare zero dependencies. Do not add lean-toolchain or [[require]] entries by hand - Cartograph manages them. No project-specific names, URLs, or environment identifiers anywhere - use generic names. Layout: lakefile.toml at the widget root ([[lean_lib]] with srcDir = \"src\", lib name = the widget slug with underscores), src/<slug>.lean holds definitions AND theorems (the house style is definition + theorem: every core function ships with at least one machine-checked property), tests/ holds test_<slug>.lean, examples/ holds example_usage.lean. Tests and examples import the module by name (import <slug>) and define main; they run via `lake env lean --run <file>`. A test's main must print ASSERT_PASS per verified runtime property and exit non-zero on failure - a clean exit with no ASSERT_PASS is rejected; proof-level assertions (example/theorem in the test file) are kernel-checked automatically when the file elaborates. Unproven `sorry`/`admit` is hard-blocked everywhere (the compiler only warns - Cartograph requires complete proofs), custom `axiom` declarations are hard-blocked in src/, and console output (IO.println/dbg_trace) and IO.sleep are blocked in src/. native_decide, unsafe, and partial defs produce warnings - prefer kernel-checked proofs and total functions; that totality is the point of a Lean widget. There is NO line-coverage requirement (no coverage tool exists for Lean; the kernel checks every theorem on every build, which is a stronger floor than coverage for proof code). Do NOT commit a lean-toolchain pin - the library validates against the machine's default elan toolchain and stamps the version at checkin."
  },
  "domain_notes": {
    "backend": "Every tunable should be a parameter: timeouts, retry counts, pool sizes, base URLs, header names. A consumer must be able to point your widget at any endpoint or database without touching source. Never hardcode URLs in src/ - accept them as parameters and demonstrate with a real URL in examples/. Return structured data (dicts, typed objects) rather than formatted strings - let the caller decide how to present it. Do not log to stdout/stderr; raise exceptions or return error values instead.",
    "data": "Transformation logic must be pure and stateless - same input always produces same output, no side effects. Accept the broadest practical input type (bytes, str, file-like, path) and let the caller choose. Expose encoding, delimiter, schema, and other format details as parameters. Raise descriptive errors on malformed input rather than silently coercing or dropping data.",
    "ml": "Expose model path, endpoint URL, and inference parameters (temperature, max_tokens, thresholds) as arguments - never hardcode them. API base URLs belong in examples/ as demonstration data, not in src/. Return raw model output or a typed result object; do not format or truncate. See language_notes for framework-specific constraints.",
    "security": "Never log secrets, tokens, or PII - not even partially. Accept credentials as parameters, never read them from environment variables or files internally. Auth endpoints and redirect URIs must be parameters - never hardcode OAuth URLs, token endpoints, or redirect URIs in src/; put real URLs in examples/ to demonstrate. Crypto parameters (key size, algorithm, salt rounds) must be explicit arguments with safe defaults. Fail closed: on any ambiguous input, reject rather than guess.",
    "infra": "Accept all paths and hostnames as parameters - never construct paths from hardcoded roots. Config keys, log formats, and health check endpoints must be configurable. Return structured status objects rather than printing; let the caller handle output. Side effects (file writes, process spawns) must be opt-in, not automatic.",
    "frontend": "All appearance values (colors, spacing, typography, shadows) must be exposed as parameters or CSS custom properties - hardcoded values lock consumers into your choices. Provide sensible defaults but every visual value must be overridable. No inline styles for anything that could vary by theme.",
    "universal": "Use this domain when a widget spans multiple domains or has no clear home — not because it must be pure. A retry utility, a date formatter, a general-purpose HTTP client all belong here. Expose variation points as parameters. Don't force it into a domain just to avoid universal.",
    "modeling": "Every dimension and tolerance must be a parameter with a sensible default - never hardcode sizes. Module names must be nouns describing the shape, not verbs. Include a parameter for fit tolerance where physical assembly is involved. All parameters must have comments explaining units (mm, degrees) and valid ranges. Default units: lengths in millimeters, angles in degrees. Widgets must render cleanly with default parameters - the example must produce a non-empty mesh. Every src/ file must declare a coordinate frame so consumers know how the part is oriented before they instantiate it: pick the system that matches the geometry (cartesian for chassis-like parts, cylindrical for rotational parts like gears or threads, spherical for radial parts like balls/lenses). State the origin and axis meanings concretely (e.g. 'Origin: bottom-back-center of the chair', '+X: forward (away from the seatback)') — generic placeholders are blocked by validation. Right-hand rule applies to all three systems. Examples should use OpenSCAD Customizer annotations (/* [Section] */ blocks with value ranges) so users can adjust parameters interactively in View > Customizer without editing code. Python sidecar (python/) is available for printability/parameter math when the .scad source can't compute something on its own — stdlib-only.",
    "rtl": "Every timing constant (clock frequency, baud rate, bit period) and bus width must be a module parameter with a default and a unit comment (// Hz, // bits, // bytes). Never hardcode these — a consumer must be able to instantiate your module at any clock frequency or bus width by changing parameters only. Module names must be nouns or noun phrases describing the hardware function (uart_tx, spi_master, gray_encoder) — not verbs. All parameters must be synthesizable types (int, logic, bit) — no real, string, or event. The example must compile cleanly and simulate to $finish with vvp. Write testbenches to be self-contained: include all required src files via the compile command, assert expected behavior, and terminate with $finish.",
    "devops": "Every variation point must be a module input variable with a type and a description - region, account, environment name, instance size, naming prefix, tags, retention. Never hardcode these - a consumer must be able to apply the module to any environment by passing different variables. Modules must not declare `provider` or `backend` blocks - those are the consumer's choice; declare only required_providers in versions.tf. Outputs must expose every identifier a downstream caller would need (resource id, ARN, endpoint, name) so the module composes cleanly. Tags must be a map(string) input with an empty default. Naming should accept a name or prefix variable rather than constructing identifiers from hardcoded literals. Examples and tests must use placeholder values (000000000000 for account IDs, example.com for domains) - real production identifiers must never appear in widget code.",
    "analog": "Every component value (resistance, capacitance, inductance, supply voltage, bias current) must be a .param with a sensible default and a unit comment — never hardcode values inline. A consumer must be able to retune the block (change cutoff, gain, rail voltage) by overriding parameters only. Block names must be nouns describing the circuit function (lowpass_rc, inverting_amp, bandgap_ref) — not verbs. Device models and vendor .lib files must be declared dependencies — local .include of machine-specific paths is blocked; use generic built-in devices (R/L/C, diode, BJT, MOSFET) by default. The testbench must drive the block with a stimulus, simulate to convergence, and assert behavior with at least one .meas directive per claimed output — a widget with no measurements is untested and is rejected. Tolerances belong in the .meas assertions (e.g. cutoff within ±5%); prefer banded asserts over exact-value checks because convergence is sensitive across simulator versions.",
    "gamedev": "Widgets must be scene-tree-agnostic: never hardcode absolute node paths (get_node(\"/root/...\"), $/AbsolutePath) or assume a specific parent — take Nodes/values as parameters or exported fields so any project can drop the script in. Game-tunable constants (speeds, cooldowns, damage, capacities) belong in exported (@export) or named const fields a consumer can set, not magic numbers buried in logic. Script names and class_name must be nouns describing the system (StateMachine, Inventory, AStarGrid) — not verbs. The test must load the script headless and assert its behavior with fake data (no real input, no rendering, no external scenes); a script with no asserted behavior is rejected. Godot 4 syntax only — deprecated Godot 3 patterns are blocked, not warned.",
    "formal": "The proof IS the product: every public definition should ship with at least one theorem stating a property a consumer would otherwise have to test for (round-trip, idempotence, bounds, monotonicity, correctness against a spec). Name theorems after the property (encode_decode, backoff_le_max), not thm1/lemma2. State theorems about the PUBLIC API, not internal helpers - a consumer should be able to cite the theorem names in their own reasoning. Avoid partial/unsafe: totality is part of the contract. If a property is too hard to prove now, ship the runtime ASSERT_PASS test and leave the theorem out entirely - never check in sorry."
  }
}
