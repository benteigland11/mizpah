diverged: brief phases own needs/deliverables (`terra brief phase <id> --needs 1-7 --deliverables 1`; `parse_index_spec`); Mizpah routes one phase at a time and closes a phase when its entries resolve
diverged: `terra known adopt --update` — a re-taken reading (voided runs, new runs on the child) replaces the parent's earlier adoption; the runs the child voided are voided on the parent
diverged: `terra brief accept/reject --reason` — the decision carries the person's reason (`decision_reason`); Mizpah's controller reads decided proposals and never re-proposes a rejected one
diverged: `terra brief propose --edit-need "N: text" / --remove-need N` (and deliverable, non-goal) — a proposal has every write a person has; removal renumbers phases and the map's `need:N` cites (a cite of the removed entry becomes `need:removed`); accept keeps the old text under the patch
diverged: gate `samples_disagree` — integer samples of one number known that disagree (runs before and after an artifact changed) fail the gate and `known link-run` warns at link time; a count does not average (1.5 letterforms from runs 0, 0, 3, 3)
diverged: `terra brief accept/reject/set --signed-by --signature` — the signature lives on the document: a decided proposal carries `signed_by`, `signature` (path of the filed mark) and `decided_at`; a brief that goes active carries `issued_by`, `issued_signature` and `issued_at`. The Mizpah app stamps these; nothing is rendered from settings at read time
diverged: brief propose/accept take --budget-points (a budget change as its own patch), 2026-09-20
diverged: the same ask is one change request — `same_ask` compares patches (budget at the same number, edit/removal at the same index, entry text equal or word-overlap ≥ 0.75); a new proposal records `same_as`; accept/reject folds every other open proposal with the same ask (`decided_with`, same decision, patch applied once), 2026-09-20
diverged: `terra brief set --crew JSON` — the models the brief is issued to run on are recorded on it (`issued_crew`, {role: {provider, model, effort}}) with the signature, and never rewritten, 2026-09-20
diverged: brief `environment` — the gym environment the project runs in, by name (`brief set --environment piano`, empty clears); the host resolves the name to a saved environment and binds it into every task
diverged: budget proposals are deltas — `brief propose --budget-delta ±N` (patch `budget_delta`, `was_budget_points` as asked, `budget_before` as applied); accept adds the delta to the budget as it stands, never writes a number over it; pre-delta `budget_points` patches still apply as written
diverged: `route complete` refuses while a Playbook walk under .playbook/open/ has an unticked step (route_walks_open; TERRA_WALKS_DIR overrides the folder)
diverged: a determined quantity (every run identical, n>=2; `distinct_sample_signatures == 1`, which upstream reported and left alone) climbs no further by runs — one method is med, high is corroboration; `known ladder` stops on it with the reason, and takes `--max-runs` (the timeout for a value that will not settle)
diverged: `route complete` refuses a claim below the bar — every owed unknown cited by a known, each known med or better, adopted above a session map ("what is not adopted did not happen")
diverged: `known depend --on file:<path>` refuses a path under a hidden directory (.tool-output, .svc, .cache, the map's tree): state or scratch a gate on the project never sees — a known depending on .tool-output/ly-compile.log was stale from the host's side however often its worker reaffirmed it (2026-09-22)
diverged: `known depend <id> --off file:<path>|known:<id>` withdraws a dependency declared by mistake — the list could only grow, and a known stale on a scratch path had no way back but a hand edit (2026-09-22)
diverged: `terra known land <unknown> [--run R] [--on SPEC] [--confidence med|high] [--no-adopt]` closes a reading in one call (link → graduate → depend → promote → adopt, stopping at the first refusal with the command past it) and `terra known replace-run <known> <old> <new>` swaps a corrected run in; route complete's refusals name `terra known land` — workers re-derived the sequence from --help a median of nine times a work order (2026-09-22)
