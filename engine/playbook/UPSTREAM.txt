diverged: a step may not link its own procedure (add-step, edit-step refuse; validate reports) — a self-link is a loop, "see step N" is prose
diverged: move-step — move a step by its unique title to a 1-based position; others keep their order (the app's procedure editor drags steps)
diverged: delete — remove a procedure from the store; refused while another procedure's step links it (the app's procedure editor)
diverged: `playbook library …` (status, touch, pin, unpin, decay, revive, tick, policy) and `.library.json` beside the store — use it or lose it: search, start/open/load and every edit stamp a touch; the host ticks the clock per green gate; what runs out of grace has decayed and is left out of search (`search --all` shows it) and out of the copy a worker takes
diverged: `open` hands back a walk of the same procedure still in progress (path, next step, unticked count) instead of writing a `-2` copy; `--again` is a deliberate second walk. `skip <file|id> --because …` marks every remaining step `[-]` and writes the reason on the file — a worker whose window rolled over re-opened its procedures, left four fresh unticked copies, and searched the CLI twice for a verb to mark them not needed
diverged: `search` is local first — unfinished walks under the working tree (`--dir`, default .) come back as `open_here` ahead of the hits, each with its next step
diverged: `tick <file|id> --done N,M --skip K` marks several steps of an open walk in one call
diverged: `edit-step` echoes the step as it now reads (`do`, linked `procedure`), not just its title
diverged: `tick` refuses to close a step that links another procedure until a walk of that procedure is closed (every box [x]/[-]); the walk header says adapt the specifics, walk the linked ones
diverged: the whole-walk `skip` verb is gone; `tick --skip N --because ...` skips one step per call with the reason written under it
diverged: a link that leads back to the linking procedure (through any chain) is refused by add-step/edit-step and reported by validate
diverged: `open` is flat by default — the procedure and everything it links as one straight list of at most 50 steps (each step names its source; cut at a boundary; `--from N` opens the next walk; `--nested` keeps the older shape); `reach` measures a method through its links; `edit-step/add-step/remove-step --walk FILE --step N` write through to the source procedure
diverged: a flat walk shows the text of the step the worker is on and the two after it; the rest open as steps are ticked (tick re-renders and returns the next step); tick closes at most six steps a call
diverged: a walk shows every step; before any tick the worker writes <walk>.plan.md (actions naming the steps they cover; steps that do not apply with why) and tick refuses a step the plan does not place
diverged: procedures carry a `widgets` field (the instruments the method calls; `edit --widgets`), listed at the top of a walk and in `reach`
diverged: the walk names its plan file's exact path (plan: …) and tick takes x.plan.md or x.md.plan.md
diverged: `create` refuses a twin (id/title share three skill stems with a procedure in the store), naming the one to improve
diverged: `playbook upstream <id>` — the chains of procedures that link down to this one, root first (how far up the waterfall it sits); `orphan` when nothing links it
