diverged: sandboxed_shell_execution bind mode — ShellConfig.workspace_dir/cache_dirs/state_dirs: /work is a bound project directory (no snapshot, no cap), caches persist outside evidence, state dirs stay tmpfs+tar with write-back; DirectoryWorkspace stands in for the tar in the file helpers; run(detached=True) for checks on a tmpfs copy with caches read-only; focused_agent_session.workspace_snapshot()
diverged: bp_focused_agent_session edit takes `edits`, a list of {old_text, new_text[, expected_occurrences]} applied in order to one file, all or none (the file is restored when one fails); 19–44% of edits were single-edit turns back to back on the same file (2026-09-23)
