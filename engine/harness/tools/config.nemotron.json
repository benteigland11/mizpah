{
  "worker": {
    "endpoint": {
      "base_url": "http://127.0.0.1:58082",
      "timeout_seconds": 900,
      "maximum_response_bytes": 16000000,
      "headers": {
        "Content-Type": "application/json"
      },
      "completion_path": "/v1/chat/completions",
      "template_path": "/apply-template",
      "tokenize_path": "/tokenize",
      "tokenizer_add_special": true,
      "tokenizer_parse_special": true
    },
    "generation": {
      "model": "nemotron-3.5-lightning",
      "reasoning_format": "deepseek",
      "reasoning_budget_tokens": 4096,
      "chat_template_kwargs": {
        "enable_thinking": true
      },
      "temperature": 0.6,
      "top_p": 0.95,
      "min_p": 0.01,
      "seed": 731
    },
    "system_prompt": "You work autonomously on the supplied assignment inside /work, usually across several context windows. Your context is small on purpose. Work the way a careful engineer works under review: one small, deliberate change at a time, each one checked before the next.\n\nBuild everything by refinement, never in one pass. For code: first write the skeleton (imports, signatures, docstrings, pass bodies), then fill in one function body, one branch, or one test per edit, running the relevant check after each. For prose such as REPORT.md: first write only the headings, then fill in one section per edit. Never write a complete implementation, test file or report in a single call, even when you can already see all of it; the skeleton is the plan, the edits are the work.\n\nNever replace a block in one call. To change a function, test or section substantially: read it, delete it with bash (sed -i 'START,ENDd' FILE using the line numbers read showed), then rebuild it by refinement like anything new. After any delete, the first write is headings or stubs only, never the finished content; then one piece per edit.\n\nTools:\n- bash: run a command. Keep commands short. Use grep -n to find where something is; use sed -i 'START,ENDd' to delete a block. Do not cat whole files, and do not re-read what you just wrote or already have.\n- read: numbered lines of one file from an offset. After grep -n, read just the function or region you are about to change.\n- write: create a file from its complete content. Use it for skeletons and short files only.\n- edit: replace exact text in an existing file. Make old_text the smallest unique span, one or two lines copied verbatim from your most recent read of that region; never use a whole function or section as old_text. Put only the new piece in new_text. If an edit reports no match or too many matches, read the region again and retry with a more precise span; if it reports the call is too large, split the change, do not shrink it.\n\nA rejected call was not executed and repeating it unchanged will be rejected again; change the approach. Long command output is saved under .tool-output/ with a preview; when output is truncated, read the saved file in ranges instead of re-running the command.\n\nVerify results before concluding. Files persist between commands; process state does not. When asked for a handoff, write your own concise continuation notes grounded in what you observed.",
    "provider": "llama_client",
    "known_issues": {
      "repetition": {
        "identical_tool_calls": 8
      }
    }
  },
  "controller": {
    "endpoint": {
      "base_url": "http://127.0.0.1:58082",
      "timeout_seconds": 900,
      "maximum_response_bytes": 16000000,
      "headers": {
        "Content-Type": "application/json"
      },
      "completion_path": "/v1/chat/completions",
      "template_path": "/apply-template",
      "tokenize_path": "/tokenize",
      "tokenizer_add_special": true,
      "tokenizer_parse_special": true
    },
    "generation": {
      "model": "nemotron-3.5-lightning",
      "reasoning_format": "deepseek",
      "reasoning_budget_tokens": 4096,
      "chat_template_kwargs": {
        "enable_thinking": true
      },
      "temperature": 0.6,
      "top_p": 0.95,
      "min_p": 0.01,
      "seed": 731
    },
    "policy_file": "controller.md",
    "context_capacity": 131072,
    "output_tokens": -1,
    "output_headroom_tokens": 12000,
    "maximum_model_calls": null,
    "maximum_tool_calls": null,
    "maximum_tool_output_characters": 24000,
    "input_target_tokens": 48000,
    "recent_review_exchanges": 2,
    "provider": "llama_client",
    "known_issues": {
      "repetition": {
        "identical_tool_calls": 8
      }
    },
    "investigation_budgets": {
      "bootstrap": 0,
      "periodic": 2,
      "completion": 6
    },
    "maximum_document_edits_per_review": 2
  },
  "session_policy": {
    "context_capacity": 65536,
    "rollover_threshold": 52000,
    "worker_output_tokens": -1,
    "handoff_output_tokens": -1,
    "output_headroom_tokens": 12000,
    "overflow_excerpt_characters": 16000,
    "reasoning_retention": "none",
    "recent_result_count": 3,
    "recent_result_characters": 12000,
    "handoff_prompt": "Write the working memory for the next context window to continue this task accurately.\n\nThe next context receives the original assignment, this memory, retained recent tool results, and access to the existing workspace and history archive. Preserve the additional information it needs to understand the current situation and choose its next action.\n\nOrganize the memory around:\n\n- **Current objective and constraints:** the user\u2019s current intent, later corrections, decisions, and boundaries that still apply.\n- **Established state:** relevant findings, changes actually made, and verification actually completed. Keep exact identifiers and values where precision matters.\n- **Unfinished work:** obligations, unresolved questions, blockers, and any action whose outcome remains uncertain.\n- **Continuation point:** what the last completed action established, what remains to be done with its result, and the next intended action with its prerequisites.\n- **Essential references:** where supporting material lives and what to retrieve there.\n\nDistinguish observed results, prior claims, assumptions, and proposed actions. Record completion only when the available evidence supports it. Preserve the reasons behind decisions when forgetting them would invite repeated mistakes.\n\nReconcile earlier memory with newer evidence into one current account. Correct superseded claims and remove resolved or irrelevant material.\n\nCondense repeated discussion and bulky outputs. Preserve information whose loss could cause incorrect work, violated constraints, or expensive reconstruction.\n\nOutput only the memory in plain Markdown. Describe pending work without performing it. Omit copied message envelopes, nested handoffs, and tool-call markup.",
    "resume_prefix": "Continue your work from your own handoff and the retained tool results. Resolve unfinished follow-up from the last completed action before moving past it. Read existing accumulated notes before updating them and preserve their established findings; retrieve the archived history when a needed detail is missing.",
    "handoff_generation_overrides": {
      "reasoning_budget_tokens": 2048
    },
    "argument_excerpt_characters": 2001
  },
  "review_policy": {
    "update_interval": 20,
    "observation_window": 10,
    "bootstrap_after_turns": 1,
    "maximum_document_characters": 256000,
    "maximum_guidance_characters": 8000,
    "maximum_request_characters": 250000,
    "execution_terms": [
      "bash",
      "sed",
      "cat <<",
      "heredoc",
      "echo",
      "chunk",
      "chunks",
      "smaller commands",
      "multiple commands",
      "size limit",
      "character limit",
      "arguments_too_large",
      "tool call",
      "tool calls",
      "run_check",
      "split the change",
      "`read`",
      "`write`",
      "`edit`",
      "read tool",
      "write tool",
      "edit tool"
    ]
  },
  "review_on_completion": true,
  "guidance_prefix": "Controller guidance:",
  "shell": {
    "bwrap": "/usr/bin/bwrap",
    "systemd_run": "/usr/bin/systemd-run",
    "systemctl": "/usr/bin/systemctl",
    "runtime_root": "/usr",
    "limits": {
      "memory_bytes": 1073741824,
      "workspace_bytes": 134217728,
      "temporary_bytes": 67108864,
      "output_bytes": 1048576,
      "visible_output_bytes": 262144,
      "processes": 64,
      "cpu_percent": 200,
      "command_seconds": 90,
      "shutdown_seconds": 5,
      "max_files": 10000
    }
  },
  "maximum_generation_retries": 2,
  "worker_tools": [
    "bash",
    "read",
    "write",
    "edit"
  ],
  "maximum_tool_argument_characters": 24000,
  "maximum_write_characters": 20000,
  "maximum_edit_characters": 12000
}
