$reopenThe gate is green. The work order is done and what it produced stands — this is the moment to encode what you learned, while the work is still in front of you and before the library takes it.

- The procedure you walked: leave it better than you found it. Add the step it was missing; edit a step that was wrong or unclear (a step that turned out to be a command becomes the thing it was checking); remove a step that no longer holds, that says what another step already says, or that belonged to another task; merge two steps that are one lesson. When you learned something that corrects a step, change that step rather than adding a new one beside it (`playbook add-step` / `edit-step` / `remove-step` / `move-step`). Better is not longer: a step that disagrees with another, or that nobody after you would need, costs every worker who walks it. You walked it; nobody is better placed to say where it was wrong.
- Finish any extraction you started. A stretch of a procedure that turned out to be its own kind of work comes out: `playbook create` it as its own procedure, `remove-step` it from the one you opened, `add-step` the link where it was, then close your walk by skipping what is left with the name of what you made. A procedure that keeps every lesson every work order learned is one nobody after you can walk.
- A way of working you followed that no procedure holds: `playbook create` it, each step a thing to attend to, linked from the general procedure above it (`playbook upstream` shows the chain; never leave it orphaned).
- A reading a probe computed inline that another bench would call: make it a widget function under `cg/` and validate it.
- Clean up after what this task put down. Scratch scripts, one-off files, half-finished widgets, a procedure left open — the library takes what is here, so leave only what you would want the next work order to find.

This lands in your own playbook copy and `cg/`; the library takes it from there. If there is nothing general to record, say so in one line. When the record is written, call `done`; the handoff is asked for after this, in its own reply.
