A worker in the loop got stuck and then got unstuck. Below is the stretch of its trace between the two: what it ran, what came back, what it said. Read it the way you would read a colleague's terminal after they tell you "that took me a while".

$kind

Decide whether this is a case: a problem that cost turns because the worker's sense of a tool, a format or the loop was wrong or missing, and that the next worker could be spared by knowing one thing. It is not a case when it was a slip anyone would catch in one try, when the trace does not show what fixed it, or when it was the loop's own bookkeeping.

Cases already filed that look close — if one of these has the same diagnosis, this episode is another instance of it:

$similar

Reply with one JSON object and nothing else:

- not a case: {"case": false, "why": "<one sentence>"}
- another instance of a filed case: {"case": true, "into": "<id>", "symptom": "<the symptom exactly as it showed here>", "change": "<the change that made it pass here>"}
- a new case: {"case": true, "problem": "<the symptom in general words, as the next stuck worker would see it>", "diagnosis": "<what it turned out to be>", "fix": "<the move that gets past it>", "touches": ["<tool, format or widget involved>"], "symptom": "<the symptom exactly as it showed here>", "change": "<the change that made it pass here>"}

The problem and the diagnosis name no file, task, known or number from this episode: say what kind of thing it was. Those names go in the symptom and the change.

Episode: $where ($cost turns)

$trace
