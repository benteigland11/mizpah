Controller guidance (from the reviewer's look at turn $issued, now turn $now; it stands until the reviewer reads the files again, which it does when you change a file it names or claim completion — if the files no longer show this, say what changed and claim):
$correction
