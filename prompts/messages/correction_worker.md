The reviewer's correction (issued $ago replies ago; it stands until the reviewer reads the files again, which it does when you change a file it names or when you claim — if the files no longer show it, say what changed and claim):
$correction
