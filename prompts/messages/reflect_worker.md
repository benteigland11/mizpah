Your window is near its threshold. Before it rolls, stand still and record the method — this is the one moment in a long task for it.

- The procedure you walked: leave it better than you found it. Add the step it was missing; edit a step that was wrong or unclear (a step that turned out to be a command becomes the thing it was checking); remove a step that no longer holds, that says what another step already says, or that belonged to another task; merge two steps that are one lesson. When you learned something that corrects a step, change that step rather than adding a new one beside it (`playbook add-step` / `edit-step` / `remove-step` / `move-step`). Better is not longer: a step that disagrees with another, or that nobody after you would need, costs every worker who walks it.
- A way of working you followed that no procedure holds: `playbook create` it, each step a thing to attend to, linked from the general procedure above it (`playbook upstream` shows the chain; never leave it orphaned).
- A stretch of a procedure you opened that turned out to be its own kind of work: take it out. `playbook create` that stretch as its own procedure, `remove-step` it from the one you opened and `add-step` the link where it was, then close your walk by skipping what is left with the name of what you made. A procedure that keeps every lesson every work order learned is one nobody after you can walk.
- A reading a probe computed inline that another bench would call: make it a widget function under `cg/` and validate it.

This lands in your own playbook copy and `cg/`; the library takes it when the work order is green. If there is nothing general to record, say so in one line. When the record is written, call `done`; the handoff is asked for after this, in its own reply.
