The Deputy's system prompt, composed from these files in name order (`mizpah.deputy.policy_text`),
each separated by a blank line. One file per subject so an edit is one subject; the numbers set the
order and leave room. `README.md` is not part of the prompt. Rules that hold across the whole prompt:
teach a method, never a number (no numeric targets); the Administrator's words and this text are the
only instructions the Deputy takes.
